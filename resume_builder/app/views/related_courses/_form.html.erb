<%= form_for(@related_course) do |f| %>
  <% if @related_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@related_course.errors.count, "error") %> prohibited this related_course from being saved:</h2>

      <ul>
      <% @related_course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course %><br />
    <%= f.text_area :course %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
