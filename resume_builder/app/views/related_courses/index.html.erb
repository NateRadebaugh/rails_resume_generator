<h1>Listing related_courses</h1>

<table>
  <tr>
    <th>Course</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @related_courses.each do |related_course| %>
  <tr>
    <td><%= related_course.course %></td>
    <td><%= link_to 'Show', related_course %></td>
    <td><%= link_to 'Edit', edit_related_course_path(related_course) %></td>
    <td><%= link_to 'Destroy', related_course, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Related course', new_related_course_path %>
<%= link_to 'Back', '/' %>
